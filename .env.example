# Optional: a TfL Unified API app key for higher rate limits.
# The app works without one (anonymous limits). Get a free key at
# https://api-portal.tfl.gov.uk/ — the proxy keeps it server-side only.
TFL_APP_KEY=

# Optional: port for the proxy + static server (default 3000).
PORT=3000
